MODEL:
  MAX_INPUT_TOKENS: 512
  MAX_OUTPUT_TOKENS: 64
  TOKENIZER_NAME: "t5-small"
  PRETRAINED_MODEL_NAME: "t5-small"
  PATH_TO_CHECKPOINT: "storage/checkpoints/epoch=4-step=12499.ckpt"

SOLVER:
  OPTIMIZER_NAME: "AdaFactor"
  BASE_LR: 0.001

WANDB:
  RUN_NAME: "Evaluate on WikiSQL Difficulty 1"
  GROUP: "Filtered Entities"
  TAGS: ["Evaluate", "WikiSQLtoToTTo", "t5-small"]
  MODE: "online"
  NOTES: "This run uses as page title and section title the table name. Does NOT use any query info."

DATASET:
  VALIDATION: "storage/datasets/totto/wikisql_to_totto/diff1_wikisql_no_query.json"
  TRAIN: "storage/datasets/totto/filtered_sampled/train.json"

DATALOADER:
  NUM_WORKERS: 11

INPUT:
  TRAIN_BATCH_SIZE: 8
  TEST_BATCH_SIZE: 8
