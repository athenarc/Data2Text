MODEL:
  MAX_INPUT_TOKENS: 512
  MAX_OUTPUT_TOKENS: 64
  TOKENIZER_NAME: "t5-small"
  PRETRAINED_MODEL_NAME: "t5-small"
  PATH_TO_CHECKPOINT: "storage/checkpoints/epoch=1-step=303.ckpt"

SOLVER:
  OPTIMIZER_NAME: "Adam"
  BASE_LR: 0.001

WANDB:
  RUN_NAME: "Train | Adam | T5-small"
  GROUP: "Filtered Entities"
  TAGS: ["DEBUG", "ToTTo", "T5", "small"]
  MODE: "offline"
  NOTES: ""

DATASET:
  VALIDATION: "storage/datasets/totto/filtered_sampled/dev.json"
  TRAIN: "storage/datasets/totto/filtered_sampled/train.json"

DATALOADER:
  NUM_WORKERS: 11

INPUT:
  TRAIN_BATCH_SIZE: 8
  TEST_BATCH_SIZE: 8
