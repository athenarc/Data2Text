MODEL:
  MAX_INPUT_TOKENS: 512
  MAX_OUTPUT_TOKENS: 64
  TOKENIZER_NAME: "t5-small"
  PRETRAINED_MODEL_NAME: "t5-small"
  PATH_TO_CHECKPOINT: "storage/checkpoints/epoch=0-step=151.ckpt"

SOLVER:
  BASE_LR: 0.001

WANDB:
  RUN_NAME: "Training run"
  TAGS: ["DEBUG", "ToTTo", "T5", "small"]
  MODE: "offline"
  NOTES: "Testing validation set run on small dataset"

INPUT:
  TRAIN_BATCH_SIZE: 8
  TEST_BATCH_SIZE: 8
